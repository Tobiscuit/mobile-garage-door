import { getPayload } from 'payload'; import configPromise from './src/payload.config'; async function clean() { const payload = await getPayload({ config: configPromise }); await payload.delete({ collection: 'users', where: { email: { equals: 'tobiasramzy@gmail.com' } } }); await payload.delete({ collection: 'users', where: { email: { equals: 'jramirez303@outlook.com' } } }); console.log('Cleaned users.'); process.exit(0); } clean();
